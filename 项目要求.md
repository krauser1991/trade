# 项目要求

生成日期：2026-06-28

## 目录

- [一、文档目录要求](#一文档目录要求)
- [二、文档生成要求](#二文档生成要求)
- [三、每日复盘视觉化输出要求](#三每日复盘视觉化输出要求)
- [四、PDF 生成要求](#四pdf-生成要求)
- [五、Git 自动提交与推送要求](#五git-自动提交与推送要求)

## 一、文档目录要求

后续在本项目中生成的所有正式文档，都必须在正文前提供目录。

目录要求：

- Markdown 文档必须包含 `## 目录`；
- 目录位置放在标题、日期、文档属性之后，正文第一章之前；
- 目录至少覆盖一级章节；长文档需要覆盖到二级章节；
- HTML、长图或 PDF 文档如果由 Markdown 转换生成，必须保留目录页或目录段落；
- 复盘、交易策略、产业链梳理、价值投资报告、会议纪要、交易体系文档都适用；
- 除非用户明确要求极短便签，否则不能省略目录。

这条规则是项目默认要求，不需要用户每次单独提醒。

## 二、文档生成要求

正式文档默认结构：

```text
# 标题

生成日期：
文档属性：

## 目录

- [一、章节](#一章节)
- [二、章节](#二章节)

## 一、章节

正文
```

交易类文档需要尽量包含：

- 市场状态；
- 资金路径；
- 核心锚点；
- 持仓或候选标的处理；
- 仓位计划；
- 风险条件；
- 次日或盘中执行清单。

## 三、每日复盘视觉化输出要求

后续生成每天的盘后复盘、每日复盘、今日操作复盘、次日操作指引时，默认不再生成 PDF。

默认交付格式：

- 优先生成深色信息图风格 HTML；
- 如用户需要图片，基于同一 HTML 截取一张竖版长图 PNG；
- HTML 输出到 `output/html/`；
- PNG 长图输出到 `output/images/`；
- 不再为每日复盘同步生成 `output/pdf/` 文件，除非用户明确要求 PDF。

视觉风格参考用户提供的两张样图：

- 深色背景，主体内容宽度控制在手机长图可读范围；
- 顶部包含标题、日期、标签和关键结论；
- 首屏放置指数、成交额、涨跌家数、连板高度、账户或仓位等关键卡片；
- 正文使用分区标题、表格、状态标签、红绿涨跌色、风险提示和行动清单；
- 卡片边框、分割线、强调色保持克制，不做花哨渐变；
- 图片或 HTML 底部必须保留“仅作个人复盘与交易计划，不构成投资建议”的风险提示。

每日复盘生成后必须检查：

- HTML 在桌面宽度和手机宽度下文字不溢出；
- 表格和正文不重叠，长股票名、长题材名能换行；
- PNG 长图如果生成，必须是完整长图，不应只截首屏；
- 文件路径需要在最终回复里明确说明。

## 四、PDF 生成要求

非每日复盘类正式文档，或用户明确要求 PDF 时，生成 PDF 必须检查：

- PDF 是否包含目录；
- 中文字体是否正常；
- 表格和正文是否重叠；
- 页数和文件路径是否明确；
- 重要文档需要渲染抽查页面后再交付。

## 五、Git 自动提交与推送要求

本项目已经由 Git 托管。后续在本项目中每次生成或更新正式文件后，默认自动完成 GitHub 推送，不需要用户再次确认。

执行要求：

- 正式 Markdown、HTML、PNG 长图、PDF、Excalidraw、CSV、资产文件、项目要求文件等，生成完成后需要纳入提交；
- 每日复盘 HTML/PNG 或 PDF 文档应在生成、基础校验、必要页面抽查完成后，再执行提交和推送；
- 临时脚本、PDF 渲染检查图、缓存文件、过程性中间文件，除非用户明确要求保留，否则不作为正式交付文件提交；
- 提交前必须查看 `git status`，确认只提交本次任务相关文件；
- 提交信息使用简洁中文，说明本次新增或更新的文档主题；
- 提交成功后自动执行 `git push` 推送到 GitHub；
- 最终回复中说明提交与推送结果，并给出关键文件路径。

这条规则是项目默认要求，不需要用户每次单独提醒。
